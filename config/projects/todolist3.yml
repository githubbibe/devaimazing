# Configuration projet todolist3

name: todolist3
description: "TODO : décrire todolist3"

# Localisation du repo projet (absolu ou ~ expansé)
repo_path: /Users/bibe/code/aimazing/todolist3

# Git
git:
  base_branch: develop
  branch_prefix: studio/     # Format studio/<slug-feature>-<hash5> (ADR 0007)
  remote: origin

# Structure du projet cible
structure:
  backend_dir: backend/
  frontend_dir: frontend/
  tests_dir: tests/
  specs_dir: specs/          # Fiches devaimazing dans le repo projet
  docs_dir: docs/

# Fichiers de référence projet (lus par le PM et l'Architecte)
reference_files:
  project_map: specs/project-map.md
  architect_map: specs/architect-map.md

# Overrides de modèles (optionnel, hérite de studio.yml si non défini)
# qwen2.5:7b-instruct (défaut studio.yml) a échoué 3/3 itérations sur l'agent
# back de gestion-taches (requirements.txt jamais créé -> ModuleNotFoundError
# pydantic en vérification, + erreur de syntaxe récurrente non corrigée
# malgré le feedback, run FAILED le 2026-08-05) — passage à 14b, machine à
# 24 Go de RAM, modèle déjà présent localement (9 Go).
models:
  agents_local: qwen2.5:14b-instruct

# Commande de test du projet cible (phase 7, agent Test). Placeholder
# {target_dir} substitué par repo_path à l'exécution. À décommenter et
# adapter une fois la stack du projet connue.
# test:
#   command: "pytest {target_dir} -q"

# Overrides de checkpoints (optionnel)
# checkpoints:
#   phase_9_audit_aval: false

# Contraintes projet spécifiques (transmises à l'Architecte) — à compléter,
# volontairement vide : pas de valeurs par défaut pertinentes pour un
# nouveau projet dont la stack n'est pas encore connue.
# project_constraints:
#   language:
#   framework_backend:
#   framework_frontend:
#   database:

# Ajouté automatiquement lors de la création du topic Telegram
# associé à ce projet (ADR 0013).
telegram:
  thread_id: 120
